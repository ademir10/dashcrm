<%- model_class = Advice -%>
<div class="page-header">
  <h1>Conselho</h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name('descrição') %>:</strong></dt>
  <dd><%= @advice.description %></dd>
  <dt><strong><%= model_class.human_attribute_name('tipo de conselho') %>:</strong></dt>
  <dd><%= @advice.type_advice %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              advices_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_advice_path(@advice), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              advice_path(@advice),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
