<h5>Selecionar Respostas que utilizarão esta tratativa</h5>

<%= form_for ([@advice, @advice.targets.build]) do |f| %>

<% if @advice.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@advice.errors.count, "error") %> prohibited this heritage from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @advice.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>
  
    <div class="col-lg-5">	
	<%= f.collection_select(:answer_id, @answers, :id, :description, {include_blank: 'Selecione a resposta desejada:'},:class => 'form-control') %>
	</div>
       		
    <%= f.submit 'Adicionar', class: 'btn btn-primary' %>
    
<% end %>



